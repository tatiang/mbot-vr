node_modules/
dist/
*.local
.DS_Store
.vite/

# Build info from TypeScript project references
*.tsbuildinfo

# Packaged deliverable zips - built on demand, not source
*.zip

# Vercel local project link/config (machine-specific, not needed in git)
.vercel
.env*

# Claude Code session-local state, not project source
.claude/scheduled_tasks.lock
